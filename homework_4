def test(text, *params, a='+4', **values):
    print(text, params, a)
    print(values)


test("Числа: ", 1, 2, 3, name='student', course='Python')

def recursion(n):
    if n == 1:
        return 1
    else:
        return n * recursion(n-1)


print(recursion(4))
